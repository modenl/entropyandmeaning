title: "从熵到意义"
book_title: "《从熵到意义》"
tagline: "一棵统一世界观的生成树：从物理到意识，从演化到意义"
description: "《从熵到意义》是一套从物理到意识、从演化到意义的统一世界观生成框架。"
source: docs
markdown: kramdown
kramdown:
  input: GFM
  math_engine: mathjax
theme: null
plugins:
  - jekyll-optional-front-matter
  - jekyll-titles-from-headings
titles_from_headings:
  enabled: true
  strip_title: true
  collections: true
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default
